# type of finite element
TypeElement = QUADRANGLE_LOBATTO

# type of equation
TypeEquation = HARMONIC_ELASTIC

# path where the meshes are stored
MeshPath = MAILLAGES/

# frequency and/or pulsation
# Frequency = a b
# sets the pulsation omega = 2*pi*a + b
Frequency = 0.91 0.0

# incident angle teta of the incident plane wave \vec{k} = (k cos teta, k sin teta)
# this angle is in degrees (0, 90, 180 by example)
IncidentAngle = 0.0

# for a regular mesh, put FileMesh = REGULAR nb_x xmin xmax ymin ymax  ref_media  ref1 ref2 ref3 ref4
# nb_x : number of subdivisions along x
# ref_media reference of the elements (quadrilaterals or triangles)
# ref1, ref2, ref3, ref4 references of the edges y = ymin, x = xmax, y = ymax and x = xmin
FileMesh = carre.mesh

# order of space approximation 
OrderDiscretization = 6

# boundary conditions
ConditionReference = 1 2 3 4 NEUMANN

# rho and sigma are defined on nodal points of the mesh
PhysicalMedia = rho 1 ISOTROPE SAME_MESH DATA/carre_elas.mesh 0 1.0 DATA/rho_elas.don 6 1.0
PhysicalMedia = sigma 1 ISOTROPE SAME_MESH DATA/carre_elas.mesh 0 1.0 DATA/sigma_elas.don 6 1.0

# isotropic case
#PhysicalMedia = C 1 ISOTROPE SAME_MESH carre.mesh 0 1.0 c0000.don 10 1.0 \
#                             SAME_MESH carre.mesh 0 1.0 c0011.don 10 1.0

# orthotropic case
#PhysicalMedia = C 1 ORTHOTROPE SAME_MESH carre.mesh 0 1.0 c0000.don 10 1.0 \
#                               SAME_MESH carre.mesh 0 1.0 c0011.don 10 1.0 \
#                               SAME_MESH carre.mesh 0 1.0 c1111.don 10 1.0 \
#                               SAME_MESH carre.mesh 0 1.0 c0101.don 10 1.0

# anisotropic case
PhysicalMedia = C 1 ANISOTROPE SAME_MESH DATA/carre_elas.mesh 0 1.0 DATA/c0000_elas.don 6 1.0 \
                               SAME_MESH DATA/carre_elas.mesh 0 1.0 DATA/c0011_elas.don 6 1.0 \
                               SAME_MESH DATA/carre_elas.mesh 0 1.0 DATA/c0001_elas.don 6 1.0 \
                               SAME_MESH DATA/carre_elas.mesh 0 1.0 DATA/c1111_elas.don 6 1.0 \
                               SAME_MESH DATA/carre_elas.mesh 0 1.0 DATA/c1110_elas.don 6 1.0 \
                               SAME_MESH DATA/carre_elas.mesh 0 1.0 DATA/c0101_elas.don 6 1.0

# Gaussian source : TypeSource = SRC_VOLUME GAUSSIAN x0 y0 radius
# where (x0, y0) is the center of the gaussian, radius is so that f(radius) = 1e-6
TypeSource = SRC_VOLUME GAUSSIAN 0.0 0.0 1.0

# oriented source
Polarization = 0.0 1.0

# automatic resolution of the linear system (selection of the optimal algorithm)
TypeSolver = AUTO

# non-linear solver used to invert Fi(X) = Y
NonLinearSolver = NEWTON 1e-15 50

# snapshots on a 2-D regular grid 200x200
SismoPlane = AUTO 200 200
# files where the diffracted and total field will be stored (extension .dat is added)
FileOutputPlane = diffracCarreElasAniso totalCarreVarAniso

# ASCII or BINARY (binary needs less memory), FLOAT or DOUBLE (simple or double precision)
OutputFormat = BINARY DOUBLE

# we want to know the solution U and gradients
ElectricOrMagnetic = -1

# put YES if you want to store the matrix on the disk
StorageMatrix = NO mat.dat

# level of display (-1 for no display, 0 for minimal display, 10 for debug ...)
PrintLevel = 2

DataNonRegressionTest = harmonic_elas2D.x 0 8
