# type of finite element
TypeElement = HEXAHEDRON_FIRST_FAMILY

# type of equation
TypeEquation = HARMONIC_MAXWELL

# path where the meshes are stored
MeshPath = MAILLAGES/

# frequency and/or pulsation
# Frequency = a b
# sets the pulsation omega = 2*pi*a + b
Frequency = 0.4 0.0

# incident angles teta and phi of the incident plane wave \vec{k} = (k sin teta cos phi, k sin teta sin phi, k cos teta)
# this angle is in degrees (0, 90, 180 by example)
IncidentAngle = 90.0 90.0

# polarization of the source (Ex Ey Ez)
Polarization = 0 0 1

# file where the mesh is stored (path is added -> MAILLAGES/DisqueCond_QUAD.mesh)
FileMesh = coneSphere_condAll.exo

# purely hexahedral mesh
TypeMesh = HEXA

# order of space approximation 
OrderDiscretization = 3

# boundary conditions
ConditionReference = 1 2 3 DIRICHLET
ConditionReference = 4 5 6 ABSORBING

# diffracted field of a plane wave exp(ikx)
# TypeSource = SRC_TOTAL_FIELD PLANE_WAVE
TypeSource = SRC_DIFFRACTED_FIELD PLANE_WAVE

# automatic resolution of the linear system (selection of the optimal algorithm)
TypeSolver = AUTO

# non-linear solver to solve Fi(X) = Y
NonLinearSolver = NEWTON 1e-15 50

# snapshots on a 2-D regular grid 200x200
SismoGrille = 0 0 0 AUTO 100 100 100
# files where the diffracted and total field will be stored (extension .dat is added)
FileOutputGrille = diffracConeMax totalConeMax

SismoMeshSurfacic = AUTO CONDITION 3
# files where the snapshots on subdivided mesh are stored
FileOutputMeshSurfacic = diffrac_surfConeMax.bb total_surfConeMax.bb

# ASCII or BINARY (binary needs less memory), FLOAT or DOUBLE (simple or double precision)
OutputFormat = BINARY DOUBLE

# we want to know the solution E and H (magnetic field)
ElectricOrMagnetic = -1

# level of display (-1 for no display, 0 for minimal display, 10 for debug ...)
PrintLevel = 3

DataNonRegressionTest = maxwell3D.x 0 8
