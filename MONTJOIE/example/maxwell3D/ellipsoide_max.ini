# type of finite element
TypeElement = TETRAHEDRON_HP_FIRST_FAMILY

# type of equation
TypeEquation = HELMHOLTZ

# path where the meshes are stored
MeshPath = MAILLAGES/

# frequency and/or pulsation
# Frequency = a b
# sets the pulsation omega = 2*pi*a + b
Frequency = 1.0 0.0

# incident angles teta and phi of the incident plane wave
# \vec{k} = (k sin teta cos phi, k sin teta sin phi, k cos teta)
# this angle is in degrees (0, 90, 180 by example)
IncidentAngle = 90.0 90.0

# file where the mesh is stored (path is added -> MAILLAGES/DisqueCond_QUAD.mesh)
FileMesh = ellipsoideVol.meshb

# order of space approximation 
OrderDiscretization = MAX_EDGE AUTO 1.5

# boundary conditions
ConditionReference = 1 DIRICHLET
ConditionReference = 2 ABSORBING

# diffracted field of a plane wave exp(ikx)
TypeSource = SRC_DIFFRACTED_FIELD PLANE_WAVE

# polarization of plane wave
Polarization = 1 0 0

# automatic resolution of the linear system (selection of the optimal algorithm)
TypeSolver = DIRECT

# snapshots on a 3-D regular grid (on three planes)
SismoGrille = 0 0 0 AUTO 100 100 100
# files where the diffracted and total field will be stored
FileOutputGrille = diffracEllipsoideMax totalEllipsoideMax

# we want to know the solution E and curl
ElectricOrMagnetic = -2

# put YES if you want to store the matrix on the disk
StorageMatrix = NO mat.dat

# level of display (-1 for no display, 0 for minimal display, 10 for debug ...)
PrintLevel = 2

DataNonRegressionTest = maxwell3D.x 0 8
